server:
  port: 8082

spring:
  application:
    name: ticket-service

management:
  endpoints:
    web:
      exposure:
        include: health,info,mappings

ticket:
  storage-dir: ./data/tickets
  public-base-url: http://localhost:8082
  download-ttl-seconds: 900